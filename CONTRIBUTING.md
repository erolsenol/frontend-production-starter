# Contributing

1. Install Node.js and pnpm.
2. Run `pnpm install`.
3. Run `pnpm check:full` before opening a pull request.
4. Keep shared contracts framework-agnostic.
5. Add tests for new behavior and update docs for public package APIs.

Prefer vertical feature slices: contract, validation, data adapter, UI, and tests should evolve together.
