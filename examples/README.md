# Examples

- `minimal-next-app`: the smallest useful starting point.
- `../apps/admin`: a complete dashboard and CRUD reference application.
- `../apps/docs`: local documentation and package overview.

Start with the minimal example unless your product already needs an authenticated admin surface.
