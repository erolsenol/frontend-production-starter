/** @deprecated Prefer @repo/http for new code. Kept as a compatibility facade. */
export { HttpError as ApiError, createHttpClient as createFetchClient } from "@repo/http";
export type { HttpClient as ApiClient } from "@repo/http";
