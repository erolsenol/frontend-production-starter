# `@repo/contracts`

Framework-neutral domain contracts shared by routes, repositories, and UI features. Keep this package free of Next.js, React, provider SDKs, and side effects.

Import stable types from here instead of duplicating response shapes inside an app.
