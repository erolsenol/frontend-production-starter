{
  "name": "frontend-production-starter",
  "private": true,
  "version": "0.6.0",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "coverage": "pnpm --dir apps/admin coverage && pnpm --dir packages/http coverage",
    "e2e": "playwright test",
    "audit": "pnpm audit --prod --audit-level=high",
    "check:full": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "verify": "pnpm check:full && pnpm coverage && pnpm e2e && pnpm audit --prod --audit-level=high",
    "generate": "node tooling/generators/generate.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@playwright/test": "^1.63.0",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "turbo": "2.11.2",
    "typescript-eslint": "^8.70.0"
  }
}
