{
  "name": "frontend-production-starter",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "check:full": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "generate": "node tooling/generators/generate.mjs"
  },
  "devDependencies": {
    "turbo": "2.5.6"
  }
}
