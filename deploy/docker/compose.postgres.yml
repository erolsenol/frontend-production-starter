services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: frontend_production_starter
      POSTGRES_USER: starter
      POSTGRES_PASSWORD: starter
    ports:
      - "5432:5432"
    volumes:
      - starter-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U starter -d frontend_production_starter"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  starter-postgres:
