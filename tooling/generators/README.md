# Generators

Generators will be added once the package APIs are stable. The intended interface is:

```bash
pnpm generate feature users
pnpm generate page settings
pnpm generate package analytics
```

Until then, use the examples and copy the smallest matching feature slice.
